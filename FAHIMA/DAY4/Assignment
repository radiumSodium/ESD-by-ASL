#define BUTTON 2 
#define LED 9  

void setup() {
  pinMode(BUTTON, INPUT); 
  pinMode(LED, OUTPUT);
}

void loop() {
  if (digitalRead(BUTTON) == HIGH) { 
    digitalWrite(LED, HIGH);  
    delay(300); 
    digitalWrite(LED, LOW);  
  }              
    else
      digitalWrite(LED, LOW);  

}
